<html>
{{#each foo}}
{{this}}
{{/each}}

<h1>Update A Customer</h1>
<form id="update-customer" action="" method="">
    First name: <input type="text" name="first_name" value="{{Customer.first_name}}"><br>
    Last name: <input type="text" name="last_name" value="{{Customer.last_name}}"><br>
    Homeworld: <select name="homeworld" id="planet-selector"> 
        {{#each Customer}}
            <option value="{{id}}">{{Customer.customerID}}</option>
        {{/each}}
    </select><br>
    Age: <input type="number" name="age" value="{{person.age}}"><br>
</form>
<button onclick="updatePerson({{person.id}})">Update</button>


