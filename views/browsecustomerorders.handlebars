<html>
{{#each foo}}
{{this}}
{{/each}}


<h2> Browse Customer Orders</h2>
<form id='filter_customer_orders' action='' method=''>
<!--- this form does not submit directly, the button handles all the interaction with backend -->
    <label for="start_date">Enter start date: </label><br>
    <input type="date" id="start_date" name="start_date" value=""><br>
    <label for="end_date">Enter end date: </label><br>
    <input type="date" id="end_date" name="end_date" value=""><br>
    <input type="button" value="Filter" onclick="filterOrdersByDate()">
</form>

<!-- <form id='search_people' action='' method=''>
<!- this form does not submit directly, the button handles all the interaction with backend -->
    <!-- <label> Search people for first name starting with:</label>
    <input id='first_name_search_string' type='text' name='first_name_search_string' value=''>
    <input type="button" value="Search" onclick="searchPeopleByFirstName()">
</form> -->
<br>
<table>
    <thead>
        <th>Date</th>
        <th>First Name</th>
        <th>Last Name</th>
        <th>Order Price</th>
    </thead>
    <tbody>
        {{#each orders}}
        <tr>
            <td>{{date}}</td>
            <td>{{First_Name}}</td>
            <td>{{Last_Name}}</td>
            <td>{{Price}}</td>
            <td><button onclick="deleteOrder({{orderID}})">Remove Order</button></td>
            <!-- <td><a href="/customers/{{customerID}}">Update Customer</a></td> -->
        </tr>
        {{/each}}
    </tbody>
</table>
</html>
